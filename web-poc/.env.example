# Local-only environment variables for web-poc
# Copy to .env and fill in values

# Local run settings
ENV=dev
DEV_USER=dev@example.com
HOST=127.0.0.1
PORT=8000

# Azure deployment settings
AZ_RG=
AZ_APP=
AZ_LOCATION=
# AZ_SUB=
